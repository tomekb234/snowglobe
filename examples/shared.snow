func f(p: !*i8) {
    *p += 1;
    print_int(*p);
}

func main() {
    var p: !*i8 = @123;

    var p1 = p;
    var p2 = p;

    print_int(*p1);

    *p2 = -123;

    print_int(*p1);

    f(p);
    f(p);
}
